#include <stdio.h>
#include <stdlib.h>
int main(int argc , char *argv[]){
    if(argc == 2){
        int number = atoi(argv[1]);
        printf("Decimal : %d",number);
        printf("Decimal : %d",number);
        printf("Decimal : %d",number);
    }else{
        
    }
    return 0;
}